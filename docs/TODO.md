# TODO
- [ ] 優化各種提示訊息 (HUD/Message Optimization)
    *   **擊殺訊息**：
        *   殺手與被害者：使用 `ActionBar` 顯示 (殺手：圖標+對象；被害者：Subtitle 顯示兇手)。
        *   其他玩家：僅在聊天室顯示極簡單行訊息 (如 `A -> B`)，移除粗體與裝飾線。
        *   **新增**：實作「連殺提示 (Kill Streak)」，在爆破模式與死鬥模式中通用。
    *   **回合狀態 (Round End Report)**：
        *   將結算畫面從聊天室多行洗版，改為 **Actionbar 單行戰後報告**。
        *   格式範例：`Round Winner: Defenders | Reason: Time-up`。
        *   將「換邊」、「回合開始」等訊息移至 `ActionBar`，配合音效反饋。
    *   **經濟結算 (Economy Feed)**：
        *   回合結算金錢 (包含擊殺獎勵、裝包獎勵、獲勝獎勵) 統一整理，在回合結束或下回合開始時，以精簡格式顯示在聊天室 (Message)。
    *   **UI 規範**：確保不影響已重新定位至右側側邊欄的 `setTitle` 顯示區域。
- [ ] 實作死鬥模式 (Deathmatch Mode)
    *   **模式定位**：作為遊戲的第二個核心模式，整合於現有架構。
    *   **地圖適配**：同一地圖支持不同模式；死鬥模式需新增 `dm_spawns` 分散重生點。
    *   **武器系統**：提供自由選槍權利 (開局自選或 Menu 選擇)。
    *   **流程優化**：跳過購買階段與 C4 邏輯，支持快速自動重生。
- [ ] 製作語言系統
    * 把前端的文字全部改成 id 格式，並用一個系統統一管理，例如 "player.death" 這樣的字串，接著每次顯示，都根據設定的語言來顯示對應的文字。
    * 好處是方便翻譯

# Maybe
- [ ] 移除經濟系統 (針對炸彈模式)
    - 若移除，需評估武器強度平衡 (如 AWP 限制)。
    - 可能與死鬥模式的「自由選槍」邏輯共用部分代碼。
- [ ] 讓玩家可以檢槍
    - 與 **移除經濟系統** 綁定評估其必要性。
- [ ] 擊殺訊息加入自定義字體圖標 (如 \uE109) 以提升質感。